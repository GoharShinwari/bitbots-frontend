import React from 'react';

const Settings: React.FC = () => {
  return (
    <div className="flex items-center justify-center h-screen bg-gray-200">
      <h1 className="text-gray-800 text-3xl font-semibold">Settings</h1>
    </div>
  );
};

export default Settings;
