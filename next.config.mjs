/** @type {import('next').NextConfig} */
const nextConfig = {
    // Enables static exporting
    output: 'export',
    trailingSlash: true, // Optional: Adds a trailing slash to the URL
  };
  
  export default nextConfig;
  